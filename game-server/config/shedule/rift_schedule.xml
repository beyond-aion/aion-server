<?xml version="1.0" encoding="UTF-8"?>
<rift_schedule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="rift_schedule.xsd">
	<!-- Eltnen -->
	<rift id="210020000">
		<open schedule="0 0 * ? * *"/>
	</rift>
	<!-- Heiron -->
	<rift id="210040000">
		<open schedule="0 0 * ? * *"/>
	</rift>
	<!-- Inggison -->
	<rift id="210050000">
		<open schedule="0 0 * ? * *"/>
		<open schedule="0 0 17 ? * FRI,MON" spawn="true"/>
		<open schedule="0 0 0 ? * THU,SUN" spawn="true"/>
	</rift>
	<!-- Cygnea -->
	<rift id="210070000">
		<open schedule="0 0 * ? * *"/>
		<open schedule="0 0 19 ? * MON,WED,SAT" spawn="true"/>
		<open schedule="0 0 15 ? * SUN" spawn="true"/>
	</rift>
	
	<!-- Morheim -->
	<rift id="220020000">
		<open schedule="0 0 * ? * *"/>
	</rift>
	<!-- Beluslan -->
	<rift id="220040000">
		<open schedule="0 0 * ? * *"/>
	</rift>
	<!-- Gelkmaros -->
	<rift id="220070000">
		<open schedule="0 0 * ? * *"/>
		<open schedule="0 0 17 ? * SUN,THU" spawn="true"/>
		<open schedule="0 0 0 ? * WED,SAT" spawn="true"/>
	</rift>
	<!-- Enshar -->
	<rift id="220080000">
		<open schedule="0 0 * ? * *"/>
		<open schedule="0 0 19 ? * TUE,THU,SUN" spawn="true"/>
		<open schedule="0 0 15 ? * SAT" spawn="true"/>
	</rift>
</rift_schedule>